import React from 'react'

const Login = () => {
  // Componente simple que actúa como placeholder para la vista de login.
  // En una app real aquí habría formularios, validación y llamadas a auth.
  return (
    <div>
      {/* Mensaje estático indicando que el usuario debe iniciar sesión */}
      <p>por favor inicia sesión</p>
    </div>
  )
}

export default Login
